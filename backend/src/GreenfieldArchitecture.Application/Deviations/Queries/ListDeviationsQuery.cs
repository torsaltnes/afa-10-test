namespace GreenfieldArchitecture.Application.Deviations.Queries;

/// <summary>
/// Query to list all deviations.
/// Kept as an empty record to allow status/severity filters to be added later
/// without changing the service signature.
/// </summary>
public sealed record ListDeviationsQuery();
